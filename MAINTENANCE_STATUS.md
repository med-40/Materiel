
---

## 7. تحديثات إضافية (بعد الإصدار الأول من هذا الملف)

### تصحيح مهم تم تطبيقه:
- `app/modules/maintenance/services.py` — دالة `complete_maintenance_record()`
  كانت لسا تستخدم `equipment_type.has_hour_meter` (الحقل القديم المحذوف).
  تم تصحيحها لاستخدام `equipment_type.measurement_unit == "hours"`.

### تحسينات إضافية على equipment_list.html (تمت خارج هذه الجلسة الموثقة):
- شريط بحث وفلاتر (نوع، وضعية، حالة فنية) تعمل بـ JavaScript على جانب العميل
- قائمة "الأعمدة" لإخفاء/إظهار أعمدة الجدول، محفوظة بـ localStorage
- عداد نتائج البحث

### ملف غير مؤكد المحتوى — يحتاج مراجعة:
- `app/modules/equipment/templates/equipment_detail.html` — ملف موجود
  فعليًا (~2.5KB، بتاريخ حديث)، لكن لم يتأكد محتواه الفعلي بسبب مشكلة نسخ
  متكررة أثناء هذه الجلسة. **افحصه مباشرة من المحرر قبل أي افتراض** — قد
  يكون بداية صفحة تفاصيل عتاد لم تكتمل، أو ملف تجريبي يمكن حذفه.
- ملف `app/modules/equipment/router.py.bak` موجود أيضًا (untracked بـ git)
  — على الأغلب نسخة احتياطية يدوية، تأكد من محتواها أو احذفها إذا لم تعد
  لازمة قبل أي commit نهائي.
