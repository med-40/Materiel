## تحديث تلقائي - 2026-08-16 05:57:08 UTC

- Commit: `394c0eb`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:50:13 UTC

- Commit: `4612796`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:31:33 UTC

- Commit: `8ba46ff`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:26:19 UTC

- Commit: `1ee6532`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:15:02 UTC

- Commit: `71f21ac`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:14:46 UTC

- Commit: `198535e`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:09:47 UTC

- Commit: `3186b9a`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:09:23 UTC

- Commit: `887633b`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:09:12 UTC

- Commit: `0228ca3`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 05:08:50 UTC

- Commit: `8f72129`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 04:57:59 UTC

- Commit: `e37bd8e`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 04:40:04 UTC

- Commit: `2d17c2c`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 04:39:04 UTC

- Commit: `d65bab7`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 04:38:51 UTC

- Commit: `b511c49`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 04:32:48 UTC

- Commit: `3e425c5`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-16 03:27:59 UTC

- Commit: `98aebe6`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 12:22:05 UTC

- Commit: `ac18373`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 12:21:48 UTC

- Commit: `30c4d76`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 12:21:36 UTC

- Commit: `6ff80f9`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 12:21:22 UTC

- Commit: `605f9e3`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:38:47 UTC

- Commit: `248332c`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:38:35 UTC

- Commit: `aac6dc4`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:37:22 UTC

- Commit: `2a18170`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:37:04 UTC

- Commit: `f541b4a`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:36:55 UTC

- Commit: `8eaf47e`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:36:27 UTC

- Commit: `accb6aa`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 07:36:08 UTC

- Commit: `6a7d180`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 06:32:10 UTC

- Commit: `6a3780c`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-15 06:05:17 UTC

- Commit: `35da68d`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-13 10:02:23 UTC

- Commit: `7fe920c`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 10:09:22 UTC

- Commit: `6dda41b`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 09:44:54 UTC

- Commit: `1d68a1d`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 08:16:37 UTC

- Commit: `4c6f66a`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 08:15:07 UTC

- Commit: `c53d9e5`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 06:14:36 UTC

- Commit: `9688184`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 06:14:25 UTC

- Commit: `a6de5b9`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 06:14:15 UTC

- Commit: `e395316`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 06:14:04 UTC

- Commit: `9c7d835`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-12 05:59:58 UTC

- Commit: `f74d1c2`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 22:45:56 UTC

- Commit: `93be319`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 22:45:06 UTC

- Commit: `a778bdc`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 22:43:33 UTC

- Commit: `4e92db5`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 22:24:26 UTC

- Commit: `dd650fb`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 22:20:38 UTC

- Commit: `f5db5b9`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 22:16:46 UTC

- Commit: `f9ddad1`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 21:57:56 UTC

- Commit: `0514b36`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 21:55:39 UTC

- Commit: `29faf23`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-11 21:31:49 UTC

- Commit: `db3b957`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-10 19:05:50 UTC

- Commit: `e60d37a`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

## تحديث تلقائي - 2026-08-10 16:56:16 UTC

- Commit: `c61bbbc`
- Branch: `main`
- منفذ التحديث: GitHub Actions

---

# Fleet & Assets Manager

نظام ويب متعدد المستخدمين لإدارة الأسطول والعتاد، مبني على معمارية معيارية
(Modular) بحيث تُضاف/تُعدّل الوحدات دون كسر بقية النظام.

## البنية

```
app/
├── core/           # النواة: إعدادات، أمان، صلاحيات، dependencies
├── database/       # اتصال قاعدة البيانات وتهيئتها فقط
├── shared/         # عناصر مشتركة عابرة للوحدات (mixins) - بدون منطق وحدة معينة
└── modules/        # كل وحدة مستقلة بذاتها:
    ├── users/          # المستخدمون، الأدوار، تسجيل الدخول
    ├── equipment/      # العتاد والسيارات
    └── dashboard/      # لوحة تحكم تجمّع مؤشرات من الوحدات الأخرى
        ├── models.py       # بيانات (SQLAlchemy)
        ├── schemas.py      # تحقق (Pydantic)
        ├── services.py     # منطق العمل
        ├── router.py       # الربط (FastAPI routes)
        └── templates/      # واجهة الوحدة (HTML خاص بها)

web/
├── main.py         # تشغيل وربط الراوترز فقط - بدون منطق عمل
└── templates/       # base.html المشترك بين كل الوحدات

static/             # CSS / JS / صور
```

## القاعدة المعمارية

- **كل وحدة تحت `modules/<name>/` تحتوي طبقاتها الأربع كاملة** (models,
  schemas, services, router) بدل تفريقها في مجلدات عامة. هذا يسمح بحذف أو
  تعطيل وحدة كاملة دون البحث في أماكن متفرقة.
- **الربط بين الوحدات يمر عبر services، لا عبر استيراد models مباشرة** قدر
  الإمكان، لتقليل الترابط (coupling).
- **إضافة وحدة جديدة** = مجلد جديد بنفس النمط + تسجيل الموديل في
  `database/init_db.py` + تسجيل الراوتر في `web/main.py`. لا تعديل على أي
  وحدة قائمة.

## التشغيل محليًا

```bash
python -m venv venv
source venv/bin/activate   # ويندوز: venv\Scripts\activate
pip install -r requirements.txt
python run_web.py
```

الدخول: `http://localhost:8000` — مستخدم افتراضي أول تشغيل: `admin / Admin@123`
(**غيّر كلمة المرور فورًا في أي بيئة غير تطوير محلي**).

## متغيرات البيئة (اختياري - للإنتاج)

| المتغير | الوصف | الافتراضي |
|---|---|---|
| `DATABASE_URL` | رابط قاعدة البيانات | `sqlite:///./fleet_assets.db` |
| `SECRET_KEY` | مفتاح تشفير الجلسات - **غيّره وجوبًا بالإنتاج** | قيمة تطوير غير آمنة |
| `APP_ENV` | `development` أو `production` | `development` |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | مدة صلاحية الجلسة بالدقائق | `480` |

## حالة الوحدات

| الوحدة | الحالة |
|---|---|
| users | ✅ مكتملة (مصادقة، أدوار) |
| equipment | ✅ مكتملة (CRUD + API) |
| dashboard | 🟡 أساسي - سيتوسع مع كل وحدة جديدة |
| drivers | ⏳ لم تُبنَ بعد |
| maintenance | ⏳ لم تُبنَ بعد |
| missions | ⏳ لم تُبنَ بعد |
| fuel | ⏳ لم تُبنَ بعد |
| faults | ⏳ لم تُبنَ بعد |
| batteries / tires | ⏳ لم تُبنَ بعد |
| spare_parts | ⏳ لم تُبنَ بعد |
| audit log (تفصيلي) | ⏳ لم يُبنَ بعد |

---

# سجل تطوير الصيانة (Maintenance Development Log)

> هذا القسم يوثّق كل ما تم إنجازه بوحدتي **الصيانة** و**العتاد** المرتبطة بها.
> اقرأه كاملًا قبل أي تعديل جديد — التطوير مرّ بعدة مراحل وتصحيحات متتالية.

## أولًا: وحدة العتاد (Equipment) — التعديلات المنجزة ✅

- **الرقم الداخلي (`asset_code`)** لم يعد يُدخل يدويًا. يتولّد تلقائيًا:
  - `EQ-{رقم_التسجيل}` إن وُجد رقم تسجيل (مثال: `EQ-jcx`)
  - `EQ-TMP-{تسلسل}` إن لم يوجد
  - المنطق: `app/modules/equipment/services.py` → `generate_asset_code()`
- **`equipment_types.measurement_unit`** حقل إجباري (`"km"` أو `"hours"`) حل محل
  الحقل القديم `has_hour_meter` (Boolean). كل نوع عتاد يحدد وحدة قياسه عند
  الإنشاء، وفورم إضافة العتاد يُظهر الحقل المناسب فقط.
- **صفحة تفاصيل عتاد فردي** (`equipment_detail.html`) — تُفتح بالضغط على عتاد
  من صفحة `/equipment`، تعرض كل بياناته بما فيها العداد المناسب لنوعه. ✅ تم
  اختبارها وتعمل.
- **تحسينات إضافية على `/equipment`**: بحث، فلاتر (نوع/وضعية/حالة فنية)،
  إخفاء/إظهار أعمدة (محفوظ بـ localStorage)، عداد نتائج.

### الملفات المتأثرة:
## ثانيًا: وحدة الصيانة (Maintenance) — مكتملة جزئيًا 🟡

### الموديلات (`app/modules/maintenance/models.py`):
- **`MaintenanceType`** — أنواع الصيانة (تغيير زيت، فحص فرامل...)
- **`MaintenanceSchedule`** — بند/خطة صيانة دوري، مرتبط بـ `equipment_type_id`
  (يُطبَّق على كل طرازات هذا النوع). فيه `interval_km`/`interval_hours`/
  `interval_days`، `internal_code` يتولّد تلقائيًا، `makes_equipment_unavailable`
- **`MaintenanceRecord`** — سجل تنفيذ فعلي (بلاغ عطل أو تنفيذ خطة مبرمجة)
- **`MeterReading`** — سجل تاريخي لقراءات العداد

### الصفحات الحالية:
| الرابط | الوصف | الحالة |
|---|---|---|
| `/maintenance` | رئيسية — 5 بطاقات + إشعارات مستحقة | ✅ |
| `/maintenance/periodic` | أوامر صيانة مفتوحة (قائمة بسيطة) | ✅ |
| `/maintenance/repairs` `/depot-day` `/annual-plan` `/inspections` | صفحات مؤقتة "قيد التطوير" | ⏳ |
| `/maintenance/settings` | إضافة أنواع/خطط صيانة (Admin) | ✅ لكن الشكل سيتغيّر — راجع "المطلوب لاحقًا" |
| `/maintenance/{id}/due` | صيانات مستحقة لعتاد معيّن | ✅ |

### وحدة الإشعارات المركزية (`app/modules/notifications/`):
تصميم Provider Pattern — كل وحدة تسجّل `register_provider()` تولّد إشعارات
بالشكل الموحّد `{key, source, severity, title, url}`. `get_all_notifications()`
تجمع من الكل، تمنع التكرار، ترتب حسب `severity` (`overdue` أحمر، `upcoming`
برتقالي). الهدف: أي وحدة مستقبلية (fuel، drivers...) تنضم تلقائيًا بدون
تعديل صفحة `/maintenance` الرئيسية.

### تصحيح مهم مُطبَّق:
`complete_maintenance_record()` بملف `services.py` كانت تستخدم الحقل القديم
`has_hour_meter` — صُحّحت لاستخدام `measurement_unit == "hours"`.

## ثالثًا: المطلوب لاحقًا 🔴 — إعادة تصميم إعدادات الصيانة

**القرار المتفق عليه:**
مرجع بصري: صورة "جدول الصيانة الدورية" (بنود بالصفوف، فترات بالأعمدة، ✓ عند
التقاطع). **هذا الجدول للعرض فقط** — حساب الاستحقاق الفعلي منطق منفصل
(`is_schedule_due()` بـ `maintenance/services.py`) يُستخدم بالإشعارات والصفحة
الرئيسية، وسيُطوَّر أكثر لاحقًا بشكل مستقل.

## دروس مستفادة (لتجنب تكرار الأخطاء)

1. النسخ اللصق من التيرمينال بالجوال غير موثوق (يتقطع/يعرض نسخ قديمة من
   الحافظة). الأفضل: `sed` على أرقام أسطر محددة (بعد `grep -n` للتأكد)، أو
   `cat > file << 'EOF'` لإعادة كتابة ملف كامل.
2. تحقق من توازن الأقواس `{}` بعد أي تعديل يدوي على JS داخل قوالب HTML —
   استخدم سكربت بايثون بسيط بدل القراءة البصرية.
3. شغّل `python3 -c "from app.modules.X import router"` قبل تشغيل السيرفر
   الكامل لكشف أخطاء الاستيراد بسرعة.
4. بعد أي تعديل على Models، احذف `fleet_assets.db` وأعد الإنشاء (بيئة تطوير،
   بيانات تجريبية) بدل migration يدوي معقّد.
5. قبل أي قرار تصميمي جوهري، اسأل: البيانات الحالية تجريبية أو مهمة؟

## كيف تكمل من هنا

اقرأ هذا القسم كاملًا، افحص الملفات المذكورة فعليًا (لا تفترض محتواها)، ثم
ابدأ من "ثالثًا: المطلوب لاحقًا" خطوة بخطوة — لا تنتقل لخطوة قبل التأكد من
نجاح السابقة (تشغيل بدون أخطاء + تجربة فعلية بالمتصفح).

---

## تحديث: قرارات تصميمية جديدة (لم يُنفَّذ الكود بعد) 🔴

### 1. تبسيط "إعدادات الصيانة"
- **حذف** مفهوم "نوع الصيانة" (`MaintenanceType`) من واجهة المستخدم كليًا —
  كان طبقة تصنيف زائدة غير ضرورية.
- **"اسم الخطة" يصبح هو البند نفسه** (مثل: "تغيير زيت المحرك") — كل خطة
  تمثّل صفًا واحدًا بالجدول المرجعي.
- **شروط التكرار متعددة ومجتمعة**: كل بند يقدر يكون له أكثر من شرط معًا
  (مثلًا: كل 10,000 كم **أو** 6 أشهر، أيهما يتحقق أولًا). هذا مدعوم أصلًا
  بالموديل الحالي (`interval_km`, `interval_hours`, `interval_days` على نفس
  السجل) — لا حاجة لتعديل الموديل، فقط تبسيط الفورم والراوترز.

### 2. صفحة "الحالة الكيلومترية/الساعات" (جديدة، لم تُبنَ بعد)
- رابط جديد داخل وحدة الصيانة: `/maintenance/meter-readings`
- **الشكل**: جدول بكل العتاد، عمود "آخر قراءة مسجّلة"، وعمود "قراءة جديدة"
  قابل للصق دفعة بيانات كاملة (مثل النسخ من إكسل) أو التعديل اليدوي خانة
  خانة — المستخدم حر بالطريقتين.
- **الصلاحية**: أي مستخدم مسجّل بالنظام (مؤقتًا، لحين بناء وحدة الصلاحيات
  المخصصة لاحقًا — انظر البند 3).
- **الوظيفة**: يستخدم دالة `record_meter_reading()` الموجودة أصلًا بـ
  `maintenance/services.py` — تحدّث `Equipment.current_odometer`/
  `current_hours` تلقائيًا + تسجّل بجدول `MeterReading` التاريخي.
- **مهم**: آخر تحديث بهذه الصفحة **يصبح المرجع** لحساب استحقاق الصيانة
  (`is_schedule_due()` تستخدم `current_odometer`/`current_hours` الحالية،
  وهذا التصميم يتوافق تلقائيًا بدون تعديل إضافي بالمنطق).
- عمود العداد بصفحة `/equipment` يتحدّث تلقائيًا لأنه نفس الحقل بقاعدة
  البيانات (`Equipment.current_odometer`/`current_hours`).

### 3. نظام الصلاحيات المخصصة — مؤجَّل لنهاية المشروع
تقرر بناء **وحدة صلاحيات منفصلة** (تحكّم دقيق لكل مستخدم بمين يقدر يسوي
إيش) في مرحلة لاحقة، بعد اكتمال باقي الوحدات الوظيفية. حاليًا نستمر
بنظام الأدوار الثابت الموجود (`admin`, `fleet_manager`, `operator`, `viewer`)
كحل مؤقت لكل الصفحات الجديدة.

### الخطوة التالية عند استئناف العمل:
ابدأ بتنفيذ البند 1 (تبسيط settings)، اختبر على السيرفر، احفظ، ثم البند 2
(صفحة القراءات)، بنفس الترتيب.

---

## تحديث: قرارات تصميمية جديدة (لم يُنفَّذ الكود بعد) 🔴

### 1. تبسيط "إعدادات الصيانة"
- **حذف** مفهوم "نوع الصيانة" (`MaintenanceType`) من واجهة المستخدم كليًا —
  كان طبقة تصنيف زائدة غير ضرورية.
- **"اسم الخطة" يصبح هو البند نفسه** (مثل: "تغيير زيت المحرك") — كل خطة
  تمثّل صفًا واحدًا بالجدول المرجعي.
- **شروط التكرار متعددة ومجتمعة**: كل بند يقدر يكون له أكثر من شرط معًا
  (مثلًا: كل 10,000 كم **أو** 6 أشهر، أيهما يتحقق أولًا). هذا مدعوم أصلًا
  بالموديل الحالي (`interval_km`, `interval_hours`, `interval_days` على نفس
  السجل) — لا حاجة لتعديل الموديل، فقط تبسيط الفورم والراوترز.

### 2. صفحة "الحالة الكيلومترية/الساعات" (جديدة، لم تُبنَ بعد)
- رابط جديد داخل وحدة الصيانة: `/maintenance/meter-readings`
- **الشكل**: جدول بكل العتاد، عمود "آخر قراءة مسجّلة"، وعمود "قراءة جديدة"
  قابل للصق دفعة بيانات كاملة (مثل النسخ من إكسل) أو التعديل اليدوي خانة
  خانة — المستخدم حر بالطريقتين.
- **الصلاحية**: أي مستخدم مسجّل بالنظام (مؤقتًا، لحين بناء وحدة الصلاحيات
  المخصصة لاحقًا — انظر البند 3).
- **الوظيفة**: يستخدم دالة `record_meter_reading()` الموجودة أصلًا بـ
  `maintenance/services.py` — تحدّث `Equipment.current_odometer`/
  `current_hours` تلقائيًا + تسجّل بجدول `MeterReading` التاريخي.
- **مهم**: آخر تحديث بهذه الصفحة **يصبح المرجع** لحساب استحقاق الصيانة
  (`is_schedule_due()` تستخدم `current_odometer`/`current_hours` الحالية،
  وهذا التصميم يتوافق تلقائيًا بدون تعديل إضافي بالمنطق).
- عمود العداد بصفحة `/equipment` يتحدّث تلقائيًا لأنه نفس الحقل بقاعدة
  البيانات (`Equipment.current_odometer`/`current_hours`).

### 3. نظام الصلاحيات المخصصة — مؤجَّل لنهاية المشروع
تقرر بناء **وحدة صلاحيات منفصلة** (تحكّم دقيق لكل مستخدم بمين يقدر يسوي
إيش) في مرحلة لاحقة، بعد اكتمال باقي الوحدات الوظيفية. حاليًا نستمر
بنظام الأدوار الثابت الموجود (`admin`, `fleet_manager`, `operator`, `viewer`)
كحل مؤقت لكل الصفحات الجديدة.

### الخطوة التالية عند استئناف العمل:
ابدأ بتنفيذ البند 1 (تبسيط settings)، اختبر على السيرفر، احفظ، ثم البند 2
(صفحة القراءات)، بنفس الترتيب.
